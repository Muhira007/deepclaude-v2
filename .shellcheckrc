# shellcheck configuration for deepclaude
# Run: shellcheck deepclaude install.sh

# We use printf '%s\n' instead of echo — suppress the warning
disable=SC3037

# Allow source directives
source-path=SCRIPTDIR
